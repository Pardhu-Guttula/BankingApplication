# Epic Title: Banking Platform — Core API

class NavigationLink:
    def __init__(self, link_id: str, title: str, url: str):
        self.link_id = link_id
        self.title = title
        self.url = url